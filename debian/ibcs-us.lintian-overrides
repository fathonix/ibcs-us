#
# ibcs-us goes to great lengths to emulate the linux kernel API,
# which of course has it's own C standard library that is also
# emulated.  As a consequnce ibcs-us doesn't need any libc
# library, so not statically linking it would be wasteful.
#
ibcs-us binary: statically-linked-binary usr/bin/ibcs-us
